<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\SIL.LCAtom.Contract\SIL.LCAtom.Contract.csproj" />
    <ProjectReference Include="..\SIL.LCAtom.Model\SIL.LCAtom.Model.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!--
      The Runner operates on an already-loaded LcmCache (it does not load or save projects; the
      Host does that — see src/SIL.LCAtom.Host/LcmUtils/FwDataProjectLoader.cs). It therefore may
      reference SIL.LCModel directly for the ICmObject/repository/unit-of-work surface, unlike
      SIL.LCAtom.Contract and SIL.LCAtom.Model, which stay LibLCM-free.
    -->
    <PackageReference Include="SIL.LCModel" Version="11.0.0-beta0150" />
  </ItemGroup>

</Project>
