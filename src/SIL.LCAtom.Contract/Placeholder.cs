namespace SIL.LCAtom.Contract;

/// <summary>
/// Stage A placeholder. The immutable Change Set contract DTOs, closed JSON serialization, RFC 8785
/// canonicalization, and intent digest utilities described in docs/architecture.md land in Stage B.
/// This project intentionally has no dependency on LibLCM.
/// </summary>
public static class ContractPlaceholder
{
}
