namespace SIL.LCAtom.Model;

/// <summary>
/// Stage A placeholder. The canonical semantic snapshot, model-surface coverage manifest, and
/// LibLCM normalization adapters described in docs/architecture.md land in Stage C.
/// </summary>
public static class ModelPlaceholder
{
}
