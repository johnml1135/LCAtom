// <auto-generated>
//   Produced by SIL.Motif.Generator's `emit` command.
//   Do NOT hand-edit. Rerun `dotnet run --project src/SIL.Motif.Generator -- emit` after a
//   manifest or MasterLCModel.xml change, and check in the result.
//
//   Source: CmPossibility.Abbreviation (basic MultiUnicode, Verbs=set|clear).
// </auto-generated>
using System;
using System.Collections.Generic;
using System.Runtime.CompilerServices;
using System.Text.Json;
using SIL.Motif.Contract.Ids;
using SIL.Motif.Contract.Model;
using SIL.Motif.Contract.Parsing;
using SIL.Motif.Model.Effects;
using SIL.Motif.Model.Snapshot;
using SIL.Motif.Runner.Snapshotting;
using SIL.LCModel;

namespace SIL.Motif.Runner.Operations;

/// <summary>Names the two <c>CmPossibility.Abbreviation</c> operation kinds.</summary>
public static class CmPossibilityAbbreviationOperationKinds
{
    public const string SetAbbreviation = "lists/cmPossibility/setAbbreviation";
    public const string ClearAbbreviation = "lists/cmPossibility/clearAbbreviation";

    [ModuleInitializer]
    internal static void Register()
    {
        OperationKindRegistry.Register(SetAbbreviation);
        OperationKindRegistry.Register(ClearAbbreviation);
        OperationHandlerRegistry.Register(SetAbbreviation, CmPossibilityAbbreviationSetHandler.Instance);
        OperationHandlerRegistry.Register(ClearAbbreviation, CmPossibilityAbbreviationClearHandler.Instance);
    }
}

/// <summary>The <c>after</c> payload for <see cref="CmPossibilityAbbreviationOperationKinds.SetAbbreviation"/>:
/// <c>{ "ws": "...", "text": "..." }</c>. Closed: any other property is rejected.</summary>
public static class CmPossibilityAbbreviationSetPayload
{
    private static readonly string[] AllowedProperties = { "ws", "text" };

    public static (string WritingSystemTag, string Text) Parse(JsonElement after)
    {
        ClosedPayloadParsing.RequireObject(after, CmPossibilityAbbreviationOperationKinds.SetAbbreviation);
        ClosedPayloadParsing.RejectUnknownProperties(after, AllowedProperties, CmPossibilityAbbreviationOperationKinds.SetAbbreviation);

        var ws = ClosedPayloadParsing.GetRequiredString(after, "ws", CmPossibilityAbbreviationOperationKinds.SetAbbreviation);
        var text = ClosedPayloadParsing.GetRequiredString(after, "text", CmPossibilityAbbreviationOperationKinds.SetAbbreviation);
        return (ws, text);
    }
}

/// <summary>The <c>after</c> payload for <see cref="CmPossibilityAbbreviationOperationKinds.ClearAbbreviation"/>:
/// <c>{ "ws": "..." }</c>. Closed: any other property (in particular <c>text</c>) is rejected.</summary>
public static class CmPossibilityAbbreviationClearPayload
{
    private static readonly string[] AllowedProperties = { "ws" };

    public static string Parse(JsonElement after)
    {
        ClosedPayloadParsing.RequireObject(after, CmPossibilityAbbreviationOperationKinds.ClearAbbreviation);
        ClosedPayloadParsing.RejectUnknownProperties(after, AllowedProperties, CmPossibilityAbbreviationOperationKinds.ClearAbbreviation);

        return ClosedPayloadParsing.GetRequiredString(after, "ws", CmPossibilityAbbreviationOperationKinds.ClearAbbreviation);
    }
}

/// <summary>Lowers <see cref="CmPossibilityAbbreviationOperationKinds.SetAbbreviation"/>: sets one
/// writing-system alternative of <c>cmPossibility.Abbreviation</c>.</summary>
public static class CmPossibilityAbbreviationSetLowering
{
    public static void Apply(LcmCache cache, ICmPossibility cmPossibility, string writingSystemTag, string text) =>
        MultiAlternativesFieldLowering.ApplySet(
            cache, cmPossibility.Abbreviation, writingSystemTag, text, CmPossibilityAbbreviationOperationKinds.SetAbbreviation);
}

/// <summary>Lowers <see cref="CmPossibilityAbbreviationOperationKinds.ClearAbbreviation"/>: clears one
/// writing-system alternative of <c>cmPossibility.Abbreviation</c>.</summary>
public static class CmPossibilityAbbreviationClearLowering
{
    public static void Apply(LcmCache cache, ICmPossibility cmPossibility, string writingSystemTag) =>
        MultiAlternativesFieldLowering.ApplyClear(
            cache, cmPossibility.Abbreviation, writingSystemTag, CmPossibilityAbbreviationOperationKinds.ClearAbbreviation);
}

/// <summary>Resolves, snapshots, lowers, and re-snapshots one
/// <see cref="CmPossibilityAbbreviationOperationKinds.SetAbbreviation"/> operation.</summary>
internal sealed class CmPossibilityAbbreviationSetHandler : IOperationHandler
{
    internal static readonly CmPossibilityAbbreviationSetHandler Instance = new();
    private CmPossibilityAbbreviationSetHandler() { }

    public ExpectedEffect ApplyAndCaptureEffect(LcmCache cache, OperationEnvelope operation, List<CanonicalId> touchedTargets)
    {
        if (operation.Target is not { })
        {
            throw new InvalidOperationException(
                $"Operation '{operation.OperationId.Value}' of kind '{CmPossibilityAbbreviationOperationKinds.SetAbbreviation}' requires 'target'.");
        }

        if (operation.After is not { } after)
        {
            throw new InvalidOperationException(
                $"Operation '{operation.OperationId.Value}' of kind '{CmPossibilityAbbreviationOperationKinds.SetAbbreviation}' requires 'after'.");
        }

        var (writingSystemTag, text) = CmPossibilityAbbreviationSetPayload.Parse(after);

        var (id, cmPossibility) = TargetResolution.Resolve<ICmPossibility>(cache, operation, CmPossibilityAbbreviationOperationKinds.SetAbbreviation);
        touchedTargets.Add(id);

        var before = ReadField(cache, cmPossibility);
        CmPossibilityAbbreviationSetLowering.Apply(cache, cmPossibility, writingSystemTag, text);
        var afterValue = ReadField(cache, cmPossibility);

        return new ExpectedEffect(id, SnapshotFields.CmPossibilityAbbreviation, before, afterValue);
    }

    public ExpectedEffect ReadCurrentFootprint(LcmCache cache, OperationEnvelope operation)
    {
        var (id, cmPossibility) = TargetResolution.Resolve<ICmPossibility>(cache, operation, CmPossibilityAbbreviationOperationKinds.SetAbbreviation);
        var current = ReadField(cache, cmPossibility);
        return new ExpectedEffect(id, SnapshotFields.CmPossibilityAbbreviation, current, current);
    }

    private static IReadOnlyDictionary<string, string> ReadField(LcmCache cache, ICmPossibility cmPossibility) =>
        CmPossibilitySnapshotter.Snapshot(cache, cmPossibility).AlternativesFields
            .TryGetValue(SnapshotFields.CmPossibilityAbbreviation, out var alternatives)
            ? alternatives
            : new Dictionary<string, string>();
}

/// <summary>The <see cref="CmPossibilityAbbreviationOperationKinds.ClearAbbreviation"/> counterpart to
/// <see cref="CmPossibilityAbbreviationSetHandler"/>.</summary>
internal sealed class CmPossibilityAbbreviationClearHandler : IOperationHandler
{
    internal static readonly CmPossibilityAbbreviationClearHandler Instance = new();
    private CmPossibilityAbbreviationClearHandler() { }

    public ExpectedEffect ApplyAndCaptureEffect(LcmCache cache, OperationEnvelope operation, List<CanonicalId> touchedTargets)
    {
        if (operation.Target is not { })
        {
            throw new InvalidOperationException(
                $"Operation '{operation.OperationId.Value}' of kind '{CmPossibilityAbbreviationOperationKinds.ClearAbbreviation}' requires 'target'.");
        }

        if (operation.After is not { } after)
        {
            throw new InvalidOperationException(
                $"Operation '{operation.OperationId.Value}' of kind '{CmPossibilityAbbreviationOperationKinds.ClearAbbreviation}' requires 'after'.");
        }

        var writingSystemTag = CmPossibilityAbbreviationClearPayload.Parse(after);

        var (id, cmPossibility) = TargetResolution.Resolve<ICmPossibility>(cache, operation, CmPossibilityAbbreviationOperationKinds.ClearAbbreviation);
        touchedTargets.Add(id);

        var before = ReadField(cache, cmPossibility);
        CmPossibilityAbbreviationClearLowering.Apply(cache, cmPossibility, writingSystemTag);
        var afterValue = ReadField(cache, cmPossibility);

        return new ExpectedEffect(id, SnapshotFields.CmPossibilityAbbreviation, before, afterValue);
    }

    public ExpectedEffect ReadCurrentFootprint(LcmCache cache, OperationEnvelope operation)
    {
        var (id, cmPossibility) = TargetResolution.Resolve<ICmPossibility>(cache, operation, CmPossibilityAbbreviationOperationKinds.ClearAbbreviation);
        var current = ReadField(cache, cmPossibility);
        return new ExpectedEffect(id, SnapshotFields.CmPossibilityAbbreviation, current, current);
    }

    private static IReadOnlyDictionary<string, string> ReadField(LcmCache cache, ICmPossibility cmPossibility) =>
        CmPossibilitySnapshotter.Snapshot(cache, cmPossibility).AlternativesFields
            .TryGetValue(SnapshotFields.CmPossibilityAbbreviation, out var alternatives)
            ? alternatives
            : new Dictionary<string, string>();
}
