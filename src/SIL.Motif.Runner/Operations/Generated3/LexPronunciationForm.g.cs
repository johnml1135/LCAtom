// <auto-generated>
//   Produced by SIL.Motif.Generator's `emit` command.
//   Do NOT hand-edit. Rerun `dotnet run --project src/SIL.Motif.Generator -- emit` after a
//   manifest or MasterLCModel.xml change, and check in the result.
//
//   Source: LexPronunciation.Form (basic MultiUnicode, Verbs=set|clear).
// </auto-generated>
using System;
using System.Collections.Generic;
using System.Runtime.CompilerServices;
using System.Text.Json;
using SIL.Motif.Contract.Ids;
using SIL.Motif.Contract.Model;
using SIL.Motif.Contract.Parsing;
using SIL.Motif.Model.Effects;
using SIL.Motif.Model.Snapshot;
using SIL.Motif.Runner.Snapshotting;
using SIL.LCModel;

namespace SIL.Motif.Runner.Operations;

/// <summary>Names the two <c>LexPronunciation.Form</c> operation kinds.</summary>
public static class LexPronunciationFormOperationKinds
{
    public const string SetForm = "lexical/lexPronunciation/setForm";
    public const string ClearForm = "lexical/lexPronunciation/clearForm";

    [ModuleInitializer]
    internal static void Register()
    {
        OperationKindRegistry.Register(SetForm);
        OperationKindRegistry.Register(ClearForm);
        OperationHandlerRegistry.Register(SetForm, LexPronunciationFormSetHandler.Instance);
        OperationHandlerRegistry.Register(ClearForm, LexPronunciationFormClearHandler.Instance);
    }
}

/// <summary>The <c>after</c> payload for <see cref="LexPronunciationFormOperationKinds.SetForm"/>:
/// <c>{ "ws": "...", "text": "..." }</c>. Closed: any other property is rejected.</summary>
public static class LexPronunciationFormSetPayload
{
    private static readonly string[] AllowedProperties = { "ws", "text" };

    public static (string WritingSystemTag, string Text) Parse(JsonElement after)
    {
        ClosedPayloadParsing.RequireObject(after, LexPronunciationFormOperationKinds.SetForm);
        ClosedPayloadParsing.RejectUnknownProperties(after, AllowedProperties, LexPronunciationFormOperationKinds.SetForm);

        var ws = ClosedPayloadParsing.GetRequiredString(after, "ws", LexPronunciationFormOperationKinds.SetForm);
        var text = ClosedPayloadParsing.GetRequiredString(after, "text", LexPronunciationFormOperationKinds.SetForm);
        return (ws, text);
    }
}

/// <summary>The <c>after</c> payload for <see cref="LexPronunciationFormOperationKinds.ClearForm"/>:
/// <c>{ "ws": "..." }</c>. Closed: any other property (in particular <c>text</c>) is rejected.</summary>
public static class LexPronunciationFormClearPayload
{
    private static readonly string[] AllowedProperties = { "ws" };

    public static string Parse(JsonElement after)
    {
        ClosedPayloadParsing.RequireObject(after, LexPronunciationFormOperationKinds.ClearForm);
        ClosedPayloadParsing.RejectUnknownProperties(after, AllowedProperties, LexPronunciationFormOperationKinds.ClearForm);

        return ClosedPayloadParsing.GetRequiredString(after, "ws", LexPronunciationFormOperationKinds.ClearForm);
    }
}

/// <summary>Lowers <see cref="LexPronunciationFormOperationKinds.SetForm"/>: sets one
/// writing-system alternative of <c>lexPronunciation.Form</c>.</summary>
public static class LexPronunciationFormSetLowering
{
    public static void Apply(LcmCache cache, ILexPronunciation lexPronunciation, string writingSystemTag, string text) =>
        MultiAlternativesFieldLowering.ApplySet(
            cache, lexPronunciation.Form, writingSystemTag, text, LexPronunciationFormOperationKinds.SetForm);
}

/// <summary>Lowers <see cref="LexPronunciationFormOperationKinds.ClearForm"/>: clears one
/// writing-system alternative of <c>lexPronunciation.Form</c>.</summary>
public static class LexPronunciationFormClearLowering
{
    public static void Apply(LcmCache cache, ILexPronunciation lexPronunciation, string writingSystemTag) =>
        MultiAlternativesFieldLowering.ApplyClear(
            cache, lexPronunciation.Form, writingSystemTag, LexPronunciationFormOperationKinds.ClearForm);
}

/// <summary>Resolves, snapshots, lowers, and re-snapshots one
/// <see cref="LexPronunciationFormOperationKinds.SetForm"/> operation.</summary>
internal sealed class LexPronunciationFormSetHandler : IOperationHandler
{
    internal static readonly LexPronunciationFormSetHandler Instance = new();
    private LexPronunciationFormSetHandler() { }

    public ExpectedEffect ApplyAndCaptureEffect(LcmCache cache, OperationEnvelope operation, List<CanonicalId> touchedTargets)
    {
        if (operation.Target is not { })
        {
            throw new InvalidOperationException(
                $"Operation '{operation.OperationId.Value}' of kind '{LexPronunciationFormOperationKinds.SetForm}' requires 'target'.");
        }

        if (operation.After is not { } after)
        {
            throw new InvalidOperationException(
                $"Operation '{operation.OperationId.Value}' of kind '{LexPronunciationFormOperationKinds.SetForm}' requires 'after'.");
        }

        var (writingSystemTag, text) = LexPronunciationFormSetPayload.Parse(after);

        var (id, lexPronunciation) = TargetResolution.Resolve<ILexPronunciation>(cache, operation, LexPronunciationFormOperationKinds.SetForm);
        touchedTargets.Add(id);

        var before = ReadField(cache, lexPronunciation);
        LexPronunciationFormSetLowering.Apply(cache, lexPronunciation, writingSystemTag, text);
        var afterValue = ReadField(cache, lexPronunciation);

        return new ExpectedEffect(id, SnapshotFields.LexPronunciationForm, before, afterValue);
    }

    public ExpectedEffect ReadCurrentFootprint(LcmCache cache, OperationEnvelope operation)
    {
        var (id, lexPronunciation) = TargetResolution.Resolve<ILexPronunciation>(cache, operation, LexPronunciationFormOperationKinds.SetForm);
        var current = ReadField(cache, lexPronunciation);
        return new ExpectedEffect(id, SnapshotFields.LexPronunciationForm, current, current);
    }

    private static IReadOnlyDictionary<string, string> ReadField(LcmCache cache, ILexPronunciation lexPronunciation) =>
        LexPronunciationSnapshotter.Snapshot(cache, lexPronunciation).AlternativesFields
            .TryGetValue(SnapshotFields.LexPronunciationForm, out var alternatives)
            ? alternatives
            : new Dictionary<string, string>();
}

/// <summary>The <see cref="LexPronunciationFormOperationKinds.ClearForm"/> counterpart to
/// <see cref="LexPronunciationFormSetHandler"/>.</summary>
internal sealed class LexPronunciationFormClearHandler : IOperationHandler
{
    internal static readonly LexPronunciationFormClearHandler Instance = new();
    private LexPronunciationFormClearHandler() { }

    public ExpectedEffect ApplyAndCaptureEffect(LcmCache cache, OperationEnvelope operation, List<CanonicalId> touchedTargets)
    {
        if (operation.Target is not { })
        {
            throw new InvalidOperationException(
                $"Operation '{operation.OperationId.Value}' of kind '{LexPronunciationFormOperationKinds.ClearForm}' requires 'target'.");
        }

        if (operation.After is not { } after)
        {
            throw new InvalidOperationException(
                $"Operation '{operation.OperationId.Value}' of kind '{LexPronunciationFormOperationKinds.ClearForm}' requires 'after'.");
        }

        var writingSystemTag = LexPronunciationFormClearPayload.Parse(after);

        var (id, lexPronunciation) = TargetResolution.Resolve<ILexPronunciation>(cache, operation, LexPronunciationFormOperationKinds.ClearForm);
        touchedTargets.Add(id);

        var before = ReadField(cache, lexPronunciation);
        LexPronunciationFormClearLowering.Apply(cache, lexPronunciation, writingSystemTag);
        var afterValue = ReadField(cache, lexPronunciation);

        return new ExpectedEffect(id, SnapshotFields.LexPronunciationForm, before, afterValue);
    }

    public ExpectedEffect ReadCurrentFootprint(LcmCache cache, OperationEnvelope operation)
    {
        var (id, lexPronunciation) = TargetResolution.Resolve<ILexPronunciation>(cache, operation, LexPronunciationFormOperationKinds.ClearForm);
        var current = ReadField(cache, lexPronunciation);
        return new ExpectedEffect(id, SnapshotFields.LexPronunciationForm, current, current);
    }

    private static IReadOnlyDictionary<string, string> ReadField(LcmCache cache, ILexPronunciation lexPronunciation) =>
        LexPronunciationSnapshotter.Snapshot(cache, lexPronunciation).AlternativesFields
            .TryGetValue(SnapshotFields.LexPronunciationForm, out var alternatives)
            ? alternatives
            : new Dictionary<string, string>();
}
