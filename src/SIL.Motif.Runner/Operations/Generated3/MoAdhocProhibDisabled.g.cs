// <auto-generated>
//   Produced by SIL.Motif.Generator's `emit` command (docs/plan-motif.md, MOT-4).
//   Do NOT hand-edit. Rerun `dotnet run --project src/SIL.Motif.Generator -- emit` after a
//   manifest or MasterLCModel.xml change, and check in the result.
//
//   Source: MoAdhocProhib.Disabled (basic Boolean, Verbs=set|clear).
// </auto-generated>
using System;
using System.Collections.Generic;
using System.Runtime.CompilerServices;
using System.Text.Json;
using SIL.Motif.Contract.Ids;
using SIL.Motif.Contract.Model;
using SIL.Motif.Contract.Parsing;
using SIL.Motif.Model.Effects;
using SIL.Motif.Model.Snapshot;
using SIL.Motif.Runner.Snapshotting;
using SIL.LCModel;

namespace SIL.Motif.Runner.Operations;

/// <summary>Names the two <c>MoAdhocProhib.Disabled</c> operation kinds.</summary>
public static class MoAdhocProhibDisabledOperationKinds
{
    public const string SetDisabled = "grammar/moAdhocProhib/setDisabled";
    public const string ClearDisabled = "grammar/moAdhocProhib/clearDisabled";

    [ModuleInitializer]
    internal static void Register()
    {
        OperationKindRegistry.Register(SetDisabled);
        OperationKindRegistry.Register(ClearDisabled);
        OperationHandlerRegistry.Register(SetDisabled, MoAdhocProhibDisabledSetHandler.Instance);
        OperationHandlerRegistry.Register(ClearDisabled, MoAdhocProhibDisabledClearHandler.Instance);
    }
}

/// <summary>The <c>after</c> payload for <see cref="MoAdhocProhibDisabledOperationKinds.SetDisabled"/>:
/// <c>{ "value": true|false }</c>. Closed: any other property is rejected.</summary>
public static class MoAdhocProhibDisabledSetPayload
{
    private static readonly string[] AllowedProperties = { "value" };

    public static bool Parse(JsonElement after)
    {
        ClosedPayloadParsing.RequireObject(after, MoAdhocProhibDisabledOperationKinds.SetDisabled);
        ClosedPayloadParsing.RejectUnknownProperties(after, AllowedProperties, MoAdhocProhibDisabledOperationKinds.SetDisabled);

        return ClosedPayloadParsing.GetRequiredBoolean(after, "value", MoAdhocProhibDisabledOperationKinds.SetDisabled);
    }
}

/// <summary>The <c>after</c> payload for <see cref="MoAdhocProhibDisabledOperationKinds.ClearDisabled"/>:
/// <c>{}</c>. Closed: any property at all is rejected — there is exactly one value to clear.</summary>
public static class MoAdhocProhibDisabledClearPayload
{
    private static readonly string[] AllowedProperties = Array.Empty<string>();

    public static void Parse(JsonElement after)
    {
        ClosedPayloadParsing.RequireObject(after, MoAdhocProhibDisabledOperationKinds.ClearDisabled);
        ClosedPayloadParsing.RejectUnknownProperties(after, AllowedProperties, MoAdhocProhibDisabledOperationKinds.ClearDisabled);
    }
}

/// <summary>Lowers <see cref="MoAdhocProhibDisabledOperationKinds.SetDisabled"/>: the one LibLCM
/// write it performs, <c>moAdhocProhib.Disabled = value</c>.</summary>
public static class MoAdhocProhibDisabledSetLowering
{
    public static void Apply(IMoAdhocProhib moAdhocProhib, bool value) => moAdhocProhib.Disabled = value;
}

/// <summary>Lowers <see cref="MoAdhocProhibDisabledOperationKinds.ClearDisabled"/>: resets
/// <c>moAdhocProhib.Disabled</c> to <c>false</c>.</summary>
public static class MoAdhocProhibDisabledClearLowering
{
    public static void Apply(IMoAdhocProhib moAdhocProhib) => moAdhocProhib.Disabled = false;
}

/// <summary>Resolves, snapshots, lowers, and re-snapshots one
/// <see cref="MoAdhocProhibDisabledOperationKinds.SetDisabled"/> operation.</summary>
internal sealed class MoAdhocProhibDisabledSetHandler : IOperationHandler
{
    internal static readonly MoAdhocProhibDisabledSetHandler Instance = new();
    private MoAdhocProhibDisabledSetHandler() { }

    public ExpectedEffect ApplyAndCaptureEffect(LcmCache cache, OperationEnvelope operation, List<CanonicalId> touchedTargets)
    {
        if (operation.Target is not { })
        {
            throw new InvalidOperationException(
                $"Operation '{operation.OperationId.Value}' of kind '{MoAdhocProhibDisabledOperationKinds.SetDisabled}' requires 'target'.");
        }

        if (operation.After is not { } after)
        {
            throw new InvalidOperationException(
                $"Operation '{operation.OperationId.Value}' of kind '{MoAdhocProhibDisabledOperationKinds.SetDisabled}' requires 'after'.");
        }

        var desired = MoAdhocProhibDisabledSetPayload.Parse(after);

        var (id, moAdhocProhib) = TargetResolution.Resolve<IMoAdhocProhib>(cache, operation, MoAdhocProhibDisabledOperationKinds.SetDisabled);
        touchedTargets.Add(id);

        var before = ReadField(cache, moAdhocProhib);
        MoAdhocProhibDisabledSetLowering.Apply(moAdhocProhib, desired);
        var afterValue = ReadField(cache, moAdhocProhib);

        return new ExpectedEffect(id, SnapshotFields.MoAdhocProhibDisabled, before, afterValue);
    }

    public ExpectedEffect ReadCurrentFootprint(LcmCache cache, OperationEnvelope operation)
    {
        var (id, moAdhocProhib) = TargetResolution.Resolve<IMoAdhocProhib>(cache, operation, MoAdhocProhibDisabledOperationKinds.SetDisabled);
        var current = ReadField(cache, moAdhocProhib);
        return new ExpectedEffect(id, SnapshotFields.MoAdhocProhibDisabled, current, current);
    }

    private static IReadOnlyDictionary<string, string> ReadField(LcmCache cache, IMoAdhocProhib moAdhocProhib) =>
        MoAdhocProhibSnapshotter.Snapshot(cache, moAdhocProhib).AlternativesFields
            .TryGetValue(SnapshotFields.MoAdhocProhibDisabled, out var alternatives)
            ? alternatives
            : new Dictionary<string, string>();
}

/// <summary>The <see cref="MoAdhocProhibDisabledOperationKinds.ClearDisabled"/> counterpart to
/// <see cref="MoAdhocProhibDisabledSetHandler"/>.</summary>
internal sealed class MoAdhocProhibDisabledClearHandler : IOperationHandler
{
    internal static readonly MoAdhocProhibDisabledClearHandler Instance = new();
    private MoAdhocProhibDisabledClearHandler() { }

    public ExpectedEffect ApplyAndCaptureEffect(LcmCache cache, OperationEnvelope operation, List<CanonicalId> touchedTargets)
    {
        if (operation.Target is not { })
        {
            throw new InvalidOperationException(
                $"Operation '{operation.OperationId.Value}' of kind '{MoAdhocProhibDisabledOperationKinds.ClearDisabled}' requires 'target'.");
        }

        if (operation.After is not { } after)
        {
            throw new InvalidOperationException(
                $"Operation '{operation.OperationId.Value}' of kind '{MoAdhocProhibDisabledOperationKinds.ClearDisabled}' requires 'after'.");
        }

        MoAdhocProhibDisabledClearPayload.Parse(after);

        var (id, moAdhocProhib) = TargetResolution.Resolve<IMoAdhocProhib>(cache, operation, MoAdhocProhibDisabledOperationKinds.ClearDisabled);
        touchedTargets.Add(id);

        var before = ReadField(cache, moAdhocProhib);
        MoAdhocProhibDisabledClearLowering.Apply(moAdhocProhib);
        var afterValue = ReadField(cache, moAdhocProhib);

        return new ExpectedEffect(id, SnapshotFields.MoAdhocProhibDisabled, before, afterValue);
    }

    public ExpectedEffect ReadCurrentFootprint(LcmCache cache, OperationEnvelope operation)
    {
        var (id, moAdhocProhib) = TargetResolution.Resolve<IMoAdhocProhib>(cache, operation, MoAdhocProhibDisabledOperationKinds.ClearDisabled);
        var current = ReadField(cache, moAdhocProhib);
        return new ExpectedEffect(id, SnapshotFields.MoAdhocProhibDisabled, current, current);
    }

    private static IReadOnlyDictionary<string, string> ReadField(LcmCache cache, IMoAdhocProhib moAdhocProhib) =>
        MoAdhocProhibSnapshotter.Snapshot(cache, moAdhocProhib).AlternativesFields
            .TryGetValue(SnapshotFields.MoAdhocProhibDisabled, out var alternatives)
            ? alternatives
            : new Dictionary<string, string>();
}
