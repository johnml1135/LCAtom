// <auto-generated>
//   Produced by SIL.Motif.Generator's `emit` command.
//   Do NOT hand-edit. Rerun `dotnet run --project src/SIL.Motif.Generator -- emit` after a
//   manifest or MasterLCModel.xml change, and check in the result.
//
//   Source: ReversalIndexEntry.PartOfSpeech (rel/atomic PartOfSpeech, Verbs=set|clear).
// </auto-generated>
using System;
using System.Collections.Generic;
using System.Runtime.CompilerServices;
using System.Text.Json;
using SIL.Motif.Contract.Ids;
using SIL.Motif.Contract.Model;
using SIL.Motif.Contract.Parsing;
using SIL.Motif.Model.Effects;
using SIL.Motif.Model.Snapshot;
using SIL.Motif.Runner.Snapshotting;
using SIL.LCModel;

namespace SIL.Motif.Runner.Operations;

/// <summary>Names the two <c>ReversalIndexEntry.PartOfSpeech</c> operation kinds.</summary>
public static class ReversalIndexEntryPartOfSpeechOperationKinds
{
    public const string SetPartOfSpeech = "lexical/reversalIndexEntry/setPartOfSpeech";
    public const string ClearPartOfSpeech = "lexical/reversalIndexEntry/clearPartOfSpeech";

    [ModuleInitializer]
    internal static void Register()
    {
        OperationKindRegistry.Register(SetPartOfSpeech);
        OperationKindRegistry.Register(ClearPartOfSpeech);
        OperationHandlerRegistry.Register(SetPartOfSpeech, ReversalIndexEntryPartOfSpeechSetHandler.Instance);
        OperationHandlerRegistry.Register(ClearPartOfSpeech, ReversalIndexEntryPartOfSpeechClearHandler.Instance);
    }
}

/// <summary>The <c>after</c> payload for <see cref="ReversalIndexEntryPartOfSpeechOperationKinds.SetPartOfSpeech"/>:
/// <c>{ "ref": "&lt;canonicalId&gt;" }</c>. Closed: any other property is rejected.</summary>
public static class ReversalIndexEntryPartOfSpeechSetPayload
{
    private static readonly string[] AllowedProperties = { "ref" };

    public static CanonicalId Parse(JsonElement after)
    {
        ClosedPayloadParsing.RequireObject(after, ReversalIndexEntryPartOfSpeechOperationKinds.SetPartOfSpeech);
        ClosedPayloadParsing.RejectUnknownProperties(after, AllowedProperties, ReversalIndexEntryPartOfSpeechOperationKinds.SetPartOfSpeech);

        return ClosedPayloadParsing.GetRequiredCanonicalId(after, "ref", ReversalIndexEntryPartOfSpeechOperationKinds.SetPartOfSpeech);
    }
}

/// <summary>The <c>after</c> payload for <see cref="ReversalIndexEntryPartOfSpeechOperationKinds.ClearPartOfSpeech"/>:
/// <c>{}</c>. Closed: any property at all is rejected -- there is exactly one value to clear.</summary>
public static class ReversalIndexEntryPartOfSpeechClearPayload
{
    private static readonly string[] AllowedProperties = Array.Empty<string>();

    public static void Parse(JsonElement after)
    {
        ClosedPayloadParsing.RequireObject(after, ReversalIndexEntryPartOfSpeechOperationKinds.ClearPartOfSpeech);
        ClosedPayloadParsing.RejectUnknownProperties(after, AllowedProperties, ReversalIndexEntryPartOfSpeechOperationKinds.ClearPartOfSpeech);
    }
}

/// <summary>Lowers <see cref="ReversalIndexEntryPartOfSpeechOperationKinds.SetPartOfSpeech"/>: resolves the
/// referenced IPartOfSpeech and assigns it to <c>reversalIndexEntry.PartOfSpeechRA</c>.</summary>
public static class ReversalIndexEntryPartOfSpeechSetLowering
{
    public static void Apply(LcmCache cache, IReversalIndexEntry reversalIndexEntry, CanonicalId refId) =>
        ReferenceFieldLowering.ApplySet<IPartOfSpeech>(
            cache, value => reversalIndexEntry.PartOfSpeechRA = value, refId, ReversalIndexEntryPartOfSpeechOperationKinds.SetPartOfSpeech);
}

/// <summary>Lowers <see cref="ReversalIndexEntryPartOfSpeechOperationKinds.ClearPartOfSpeech"/>: detaches
/// <c>reversalIndexEntry.PartOfSpeechRA</c>.</summary>
public static class ReversalIndexEntryPartOfSpeechClearLowering
{
    public static void Apply(IReversalIndexEntry reversalIndexEntry) =>
        ReferenceFieldLowering.ApplyClear<IPartOfSpeech>(value => reversalIndexEntry.PartOfSpeechRA = value);
}

/// <summary>Resolves, snapshots, lowers, and re-snapshots one
/// <see cref="ReversalIndexEntryPartOfSpeechOperationKinds.SetPartOfSpeech"/> operation.</summary>
internal sealed class ReversalIndexEntryPartOfSpeechSetHandler : IOperationHandler
{
    internal static readonly ReversalIndexEntryPartOfSpeechSetHandler Instance = new();
    private ReversalIndexEntryPartOfSpeechSetHandler() { }

    public ExpectedEffect ApplyAndCaptureEffect(LcmCache cache, OperationEnvelope operation, List<CanonicalId> touchedTargets)
    {
        if (operation.Target is not { })
        {
            throw new InvalidOperationException(
                $"Operation '{operation.OperationId.Value}' of kind '{ReversalIndexEntryPartOfSpeechOperationKinds.SetPartOfSpeech}' requires 'target'.");
        }

        if (operation.After is not { } after)
        {
            throw new InvalidOperationException(
                $"Operation '{operation.OperationId.Value}' of kind '{ReversalIndexEntryPartOfSpeechOperationKinds.SetPartOfSpeech}' requires 'after'.");
        }

        var refId = ReversalIndexEntryPartOfSpeechSetPayload.Parse(after);

        var (id, reversalIndexEntry) = TargetResolution.Resolve<IReversalIndexEntry>(cache, operation, ReversalIndexEntryPartOfSpeechOperationKinds.SetPartOfSpeech);
        touchedTargets.Add(id);

        var before = ReferenceFieldSnapshotting.ReadAlternatives(reversalIndexEntry.PartOfSpeechRA);
        ReversalIndexEntryPartOfSpeechSetLowering.Apply(cache, reversalIndexEntry, refId);
        var afterValue = ReferenceFieldSnapshotting.ReadAlternatives(reversalIndexEntry.PartOfSpeechRA);

        return new ExpectedEffect(id, SnapshotFields.ReversalIndexEntryPartOfSpeech, before, afterValue);
    }

    public ExpectedEffect ReadCurrentFootprint(LcmCache cache, OperationEnvelope operation)
    {
        var (id, reversalIndexEntry) = TargetResolution.Resolve<IReversalIndexEntry>(cache, operation, ReversalIndexEntryPartOfSpeechOperationKinds.SetPartOfSpeech);
        var current = ReferenceFieldSnapshotting.ReadAlternatives(reversalIndexEntry.PartOfSpeechRA);
        return new ExpectedEffect(id, SnapshotFields.ReversalIndexEntryPartOfSpeech, current, current);
    }
}

/// <summary>The <see cref="ReversalIndexEntryPartOfSpeechOperationKinds.ClearPartOfSpeech"/> counterpart to
/// <see cref="ReversalIndexEntryPartOfSpeechSetHandler"/>.</summary>
internal sealed class ReversalIndexEntryPartOfSpeechClearHandler : IOperationHandler
{
    internal static readonly ReversalIndexEntryPartOfSpeechClearHandler Instance = new();
    private ReversalIndexEntryPartOfSpeechClearHandler() { }

    public ExpectedEffect ApplyAndCaptureEffect(LcmCache cache, OperationEnvelope operation, List<CanonicalId> touchedTargets)
    {
        if (operation.Target is not { })
        {
            throw new InvalidOperationException(
                $"Operation '{operation.OperationId.Value}' of kind '{ReversalIndexEntryPartOfSpeechOperationKinds.ClearPartOfSpeech}' requires 'target'.");
        }

        if (operation.After is not { } after)
        {
            throw new InvalidOperationException(
                $"Operation '{operation.OperationId.Value}' of kind '{ReversalIndexEntryPartOfSpeechOperationKinds.ClearPartOfSpeech}' requires 'after'.");
        }

        ReversalIndexEntryPartOfSpeechClearPayload.Parse(after);

        var (id, reversalIndexEntry) = TargetResolution.Resolve<IReversalIndexEntry>(cache, operation, ReversalIndexEntryPartOfSpeechOperationKinds.ClearPartOfSpeech);
        touchedTargets.Add(id);

        var before = ReferenceFieldSnapshotting.ReadAlternatives(reversalIndexEntry.PartOfSpeechRA);
        ReversalIndexEntryPartOfSpeechClearLowering.Apply(reversalIndexEntry);
        var afterValue = ReferenceFieldSnapshotting.ReadAlternatives(reversalIndexEntry.PartOfSpeechRA);

        return new ExpectedEffect(id, SnapshotFields.ReversalIndexEntryPartOfSpeech, before, afterValue);
    }

    public ExpectedEffect ReadCurrentFootprint(LcmCache cache, OperationEnvelope operation)
    {
        var (id, reversalIndexEntry) = TargetResolution.Resolve<IReversalIndexEntry>(cache, operation, ReversalIndexEntryPartOfSpeechOperationKinds.ClearPartOfSpeech);
        var current = ReferenceFieldSnapshotting.ReadAlternatives(reversalIndexEntry.PartOfSpeechRA);
        return new ExpectedEffect(id, SnapshotFields.ReversalIndexEntryPartOfSpeech, current, current);
    }
}
